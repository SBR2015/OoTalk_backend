<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= image_tag "ootalk_logo.png" %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
		    <li><%= link_to 'Code', code_path, id: "Code" %></li>
        <li><%= link_to 'Input JSON', codejson_path, id: "Input_JSON" %></li>
        <li><a href="#" id="Courses">Courses</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="Language">Language
          <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'English', "#{request.path}" %></li>
            <li><%= link_to '日本語', "#{request.path}?lang=ja"%></li>
            <li><%= link_to '中文', "#{request.path}?lang=cn" %></li>
            <li><%= link_to 'Tiếng Việt', "#{request.path}?lang=vi" %></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="Menu">Menu<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if user_signed_in? %>
			<% if current_user.admin? %>
			<li><%= link_to 'admin', rails_admin_path %></li>
			<% end %>
			<!-- <%= current_user.email if user_signed_in? %> -->
              <li><%= link_to t('devise.registrations.edit_profile'), edit_user_registration_path %></li>
              <li><%= link_to t('devise.sign_out'), destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to t('devise.registrations.sign_up'), new_user_registration_path %></li>
              <li><%= link_to t('devise.sessions.sign_in'), new_user_session_path %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
    <%= render 'layouts/partials/navbar_tail' %>
  </div><!-- /.container-fluid -->
</nav>
