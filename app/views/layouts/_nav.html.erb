<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= image_tag "ootalk_logo.png" %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/codejson"><%= t('Input JSON') %></a></li>
        <li><a href="#" id="subject">Courses</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= t('Language') %>
          <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to_if params[:lang] != 'en', 'English', "#{request.path}?lang=en" %></li>
            <li><%= link_to_if params[:lang].present?, '日本語', "#{request.path}"%></li>
            <li><%= link_to_if params[:lang] != 'cn', '中文', "#{request.path}?lang=cn" %></li>
            <li><a href="/?lang=vi">Tiếng Việt</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= t('Menu') %><span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if user_signed_in? %>
			<% if current_user.admin? %>
			<li><a href="/admin">admin</a></li>
			<% end %>
			<!-- <%= current_user.email if user_signed_in? %> -->
              <li><%= link_to t('devise.registrations.edit_profile'), edit_user_registration_path %></li>
              <li><%= link_to t('devise.sign_out'), destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to t('devise.registrations.sign_up'), new_user_registration_path %></li>
              <li><%= link_to t('devise.sessions.sign_in'), new_user_session_path %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
    <%= render 'layouts/partials/navbar_tail' %>
  </div><!-- /.container-fluid -->
</nav>
